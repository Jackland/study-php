##### 以下默认是buyer端
##### seller端只要用main-content替换类名：buyer-page
![image](https://note.youdao.com/yws/api/personal/file/WEBc02add22bc425711065c98b3e9a1b0d7?method=download&shareKey=a1b2aa773e7eae4a2ed2213b3f5f9a26)
```
{{ this.title('前端Buyer示例页面') }}
{{ this.params('breadcrumbs', [
'home',
'current'
]) }}
{{ this.params('pageTitle', {backUrl: false}) }}
{{ jsOrigin(['/catalog/view/javascript/bootstrap/js/bootstrap-paginator.min.js']) }}

{# TODO===>  {{ css(['..相对于public下文件路径/index.css']) }}  #}
{# 变量：frontendExamPage，只是示例，需要开发重新自行命名 #}
{% set list=[{id: 1, a:'a1', b:'b1'},{id: 2, a:'a2',b:'b2'}] %}
<div id="frontendExamPage" class="buyer-page">
  <div class="oris-row">
    <div class="oris-col-3">
      <label for="searchName">Name</label>
      <input type="text" name="searchName" placeholder="Please Input" id="searchName" class="oris-input" autocomplete="off" value="" />
    </div>
    <div class="oris-col-6 search-btn m24-t">
      <button id="filterBtn" type="button" class="oris-button oris-button-bigger width80" data-toggle="tooltip" title="Filter">Filter</button>
      <button type="button" class="oris-button oris-button-default" data-toggle="tooltip" title="Download">
          <i class="giga icon-xiazai1"></i>
      </button>
    </div>
  </div>
  <table class="oris-table m20-t">
    <thead>
      <tr>
        <th class="oris-w120">Aa</th>
        <th class="oris-w100">Bb</th>
        <th class="text-left oris-w100">Action</th>
      </tr>
    </thead>
    <tbody>
      {% if list|length %}
      {% for e,item in list %}
      <tr>
        <td>{{ item.a }}</td>
        <td>{{ item.b }}</td>
        <td class="text-left">
          <a href="javascript:void(0)" data-index="{{e}}" class="btn-text action-edit">
            <span><i class="giga icon-seller_bianji-01" data-toggle="tooltip" title="Edit"></i></span>
          </a>&nbsp;
          <a href="javascript:void(0)" data-id="{{item.id}}" class="btn-text action-delete">
            <span>
              <i class="giga icon-seller_lajitong-01" data-toggle="tooltip" title="Delete"></i>
            </span>
          </a>
        </td>
      </tr>
      {% endfor %}
      {% else %}
      <tr class="no-records">
        <td colspan="3">No records.</td>
      </tr>
      {% endif %}
    </tbody>
  </table>
  {% if list|length %}
  {# 分页 #}
  <div class="oris-row oris-pagination">
    <ul id="tablePagination"></ul>
  </div>
  {% endif %}
</div>
{# include 按需引入，没有弹框不需要 #}
{# TODO===> {% include '(相对于twig文件下的components)/components/detailModal.twig' %} #}
{% apply script %}
<script type="text/javascript">
$(function() {
  frontendExamPage.initComponents();
  frontendExamPage.bindClick();
});
var frontendExamPage = {
  $modal: $('#detailModal'),
  data: {
    modalData: { // 弹框的数据
      id: 0,
      name: ''
    },
    // list: {{ list }}, // 列表数据
    tipstext: { // 所有提示语句 
      name: '请输入正确的名称',
    }
  },
  // 初始化组建
  initComponents: function() {
    block.unBlockUI();
    // 初始化分页
    $('#tablePagination').bootstrapPaginator({
      bootstrapMajorVersion: 3,
      size: 'normal',
      currentPage: {{ paginator.getPage() }},
      totalPages: {{ max(paginator.getPageCount(), 1) }},
      numberOfPages: 5,
      pageRange: [10, 20, 30, 50, 100],
      rowsOfPage: {{ paginator.getPageSize() }},
      total: {{ paginator.getTotalCount() }},
      onPageClicked: function(event, originalEvent, type, page) {
        block.blockUI();
        this.loadPage('{{ paginator.getUrlWithNoPage() }}&page_limit=' + originalEvent['page_info']['page_limit'] + '&page=' + page);
      }
    });
  },
  bindClick: function() {
    // 页面初始化绑定click事件
  },
  loadPage: function(url) {
    block.blockUI();
    window.location.href = url;
  },
  // 举例保存弹框内容
  submitService: function() {
    if (feValidator.do()) {
      return;
    }
    // 发送请求
    block.blockUI();
    $.ajax({
      url: 'index.php?route=test/test',
      data: data.modalData,
      method: 'post',
      cache: false,
      dataType: 'json',
      beforeSend: function() {
        // 如果需要禁用按钮btn...
      },
      complete: function() {
        // 如果需要取消禁用按钮btn...
      },
      success: function(res) {
        // 成功...
      },
      error: function(xhr, ajaxOptions, thrownError) {
        // 失败...
        // alert(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);
      }
    })
  },
  doValidator: function() {
    var result = false;
    // 以下举例多个字段校验
    // result = this.buyerIsInvalid() || result;
    // result = this.effectiveIsInvalid() || result;
    return this.nameIsInvalid() || result;
  },
  // 举例校验
  nameIsInvalid: function() {
    var result = false;
    var name = $('#modalname').val();
    // 随便举例的校验
    var regx = /^(?:|[1-6][0-9]|70|[1-9])$/;
    if (!regx.test(name)) {
      // 红色错误提示
      $('#modalname').siblings('.text-danger').html(this.data.tipstext.name);
      return true;
    }
    // 每次校验成功后存入数据，就不用每次提交数据前重新整理了，
    // 建议数据的整理在校验的时候直接做了
    this.data.modalData.name = name;
    $('#modalname').siblings('.text-danger').html('');
    return result;
  }
};
</script>
{% endapply %}
```