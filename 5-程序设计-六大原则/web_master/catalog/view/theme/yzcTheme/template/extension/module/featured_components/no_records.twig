{% macro no_record(one,two,three) %}
  {% if one|default(0) == 0 and two|default(0) == 0 and three|default(0) == 0 %}
    <div class="main">
      <div class="center-module">
        <div class="no-records">
          <img src="/public/image/channel/kong.png"/>
          <p class="records-title">No records</p>
        </div>
        <style>
          .no-records {
            text-align: center;
            margin-top: 100px;
            margin-bottom: 120px;
          }

          .records-title {
            font-size: 16px;
            color: #333;
            margin-top: 20px;
          }
        </style>
      </div>
    </div>
  {% endif %}
{% endmacro %}